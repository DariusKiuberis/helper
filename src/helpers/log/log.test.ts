import log from './log'

describe('log() test', () => {
  test('should log value if pass something', () => {
    const input = 'some text'
    const output = ['some text']

    expect(2 + 2).toEqual(4)
  })
})
